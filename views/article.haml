- content_for :head do
  %link{:rel => 'stylesheet', :href => '/stylesheets/article.css', :type => 'text/css'}
  :javascript
    $(function(){ $('title').text( $('h1').first().text() + " | César Salazar, Observaciones de un explorador" ); })
#content
  %h4 
    %a{:href => "/"}Observaciones de un explorador
  %article
    = @content
      